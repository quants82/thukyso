import { passPrompt } from "./common.js";

export const RISKS_PROMPT = passPrompt(
  "điểm quan trọng cần kiểm tra",
  "Nhận diện rủi ro, căn cứ pháp lý, tài chính, trách nhiệm, thời hạn và điểm cần con người kiểm tra."
);
